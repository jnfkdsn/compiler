经历了上一轮拔除全局变量（引入 dispatcher.py）和剥离算子硬编码的重构后，目前这套架构在**前端追踪（Tracing）模型上已经具备了非常清晰的雏形**。但是，如果用工业级编译器（如 PyTorch Inductor, TVM, JAX）的标准来看，项目里目前仍然存在几个 **“设计断层”** 和 **“值得大改的痛点”**，主要集中在以下四个方面：

### 1. 最大的不合理：有两套互不相通的 Autograd 规则，维护成本翻倍
*   **现状**：
    *   在 dispatcher.py 中，你维护了 `BINARY_GRAD` 和 `UNARY_GRAD` 字典，里面写的是基于 NumPy 的 Eager 梯度运算（比如 `g * r.T`）。这是为了服务 `Tensor.backward()` 动态执行的。
    *   但在 train_jit.py 里的 `build_backward_graph` 函数，你又写了一个庞大的 `for n in reversed(ordered):` 循环，**把加减乘除所有的求导数学公式用图节点 `_add_binary()` 重新硬塞了一遍**。
*   **为什么不合理**：一旦未来引入新算子（比如 Conv2D 或 LayerNorm），开发者不仅要在 dispatcher 写一遍 eager 反向传播，还要在 train_jit 建图代码里再写一遍符号求导。一旦写错，两边行为就不一致了。
*   **改进思路（统一 VJP）**：参考 JAX 的设计，应该引入统一的 **VJP (Vector-Jacobian Product)** 抽象。反向传播规则应该只定义一次（基于高层 `Tensor` API）。不管是 Eager 执行还是 Graph Tracing，统统调用这一套高阶规则。既然 Tracing 时一切行为都会被录制成计算图，理论上 `backward` 图生成可以直接复用 eager 下的数学公式规则。
### 3. 动态 Shape 风暴：循环上界严重硬编码
*   **现状**：在 JIT 编译时（如 `nn/jit.py` 和 CodeGen），你的 C++ 循环条件写的是具体的数字：`cond=f"j < {n}"`。缓存的 key 严格等于 `(x_shape, y_shape)`。
*   **为什么不合理**：如果你的 Batch Size 是动态的（比如大模型推理，有时是 32，最后那个 batch 是 16），那么改变一次 shape，系统就要触发整个 `clang++` 后端的**重新编译**。这在某些 NLP 轮循中会导致灾难性的耗时（Re-compilation Explosion）。
*   **改进思路（Symbolic Shape）**：编译时生成的 C++ 代码应该从硬编码 `j < 32` 变成去读传入的尺寸：`j < input_0.shapes[0]`。只要张量的 **Rank (维度数)** 没有变，就不需要重新触发 C++ 编译。

### 4. 运行时内存系统太弱：还在 C++ 侧动态堆分配 
*   **现状**：你的 `Buffer Pool` 复用虽然设计了生命周期统计，但这发生在生成的代码或者运行时内部。
*   **改进思路（Arena Workspace）**：现代框架是不允许内核计算时再去 `malloc` 甚至寻找 free-slot 的。在 Python 中端 `Pass` 分析完毕后，应该直接算出整张图最大需要的瞬时峰值内存长度（即 Memory Planner Planner Offset 算法），然后在 Python runtime 里**一次性申请一大块连续的 Memory Arena (Blob)**，把它作为指针 `void* workspace` 传给 C++。C++ kernel 里原本用来存放 `node_5` 的临时变量，全部换成 `float* n5 = workspace + offset_n5;`。这样能在底层彻底做到真正的 “Zero-Allocation运行时”。


3. **消除屎山代码**：设计统一的 Autograd `def backward_rule(op_type, grad, ops, ...)` 装饰器，干掉当前重叠的两套反向传播代码。



我要把里程碑拆成可执行的 PR 任务并写入 TODO 列表，随后给出每个 PR 的代码位置与示例改动。

Added 7 todos

下面是按 PR 级别拆出的实现步骤（每项含目标、修改位置与示例改动）。我把优先级和预估工作量也一并给出，便于在简历中选择要突出项。

PR-0（设计）：全局惰性 IR 与执行器草图
- 目标：写设计文档，定义全局 IR、子图签名、形状 guard、执行模型与缓存键。
- 改动位置：新文件 examples/tensor_cpu/docs/lazy_design.md（创建）。
- 示例内容：总体架构图 + 子图签名示例 + 编译缓存 key 格式（hash(node ops + shape constraints)）。
- 优先级：高，工作量：0.5-1 天。

PR-1（基础）：扩展 `LazyTensor` 与延迟构图
- 目标：让 `LazyTensor` 只记录操作而不立即 materialize，保持 TraceContext 能捕获 lazy graph。
- 修改位置：
  - lazy.py（扩展/修改 LazyTensor）
  - tracer.py（确保 TraceContext 可捕获 lazy nodes）
- 示例改动（伪代码）：
  - 将 LazyTensor 操作实现改为记录 op + 输入 id，而不是调用即时 op。
- 优先级：高，工作量：1-2 天。

PR-2（API）：实现 `materialize()`（Lazy → Graph）
- 目标：提供 `LazyTensor.materialize()` 将延迟表达式转换为 `Graph`（利用现有 tracing 逻辑）。
- 修改位置：
  - lazy.py
  - 可能小幅修改 tracer.py
- 示例：新增 `materialize()`，内部 `with TraceContext(): replay_ops()` 并返回 `tc.graph`。
- 优先级：高，工作量：0.5-1 天.

PR-3（捕获）：全局捕获与子图划分
- 目标：实现在训练循环入口捕获整段计算并分割为可编译子图（按依赖、device、simple heuristics）。
- 修改位置：
  - 新文件 examples/tensor_cpu/capture.py
  - 调整 jit.py 使用全局 capture。
- 示例改动：添加 capture API：start_capture()/end_capture() 返回 Graph。
- 优先级：中高，工作量：1-2 天。

PR-5（融合）：pattern-based fusion（elementwise chain / matmul fusion）
- 目标：实现简单的 fusion pass，把元素级链和 matmul+bias+relu 融合成单个节点/子图。
- 修改位置：
  - [examples/tensor_cpu/passes/fusion.py]（扩展已有）
  - 触发点：编译 pipeline 调用 fusion。
- 示例改动：在 fusion.py 中新增 pattern matcher（查找连续 elementwise 序列并合并成 FUSED_* 节点）。
- 优先级：高，工作量：2-4 天。

PR-6（内存）：liveness 分析 + workspace slot allocation
- 目标：实现中间值生命期分析，基于此改进 `CppCodegen` 的槽复用策略并输出 `workspace_slots`。
- 修改位置：
  - 新文件 [examples/tensor_cpu/passes/liveness.py]
  - 修改 [examples/tensor_cpu/codegen.py]（`generate()` 中使用 liveness info）
- 示例改动：在 codegen 生成阶段，把 `use_count` 换成基于 liveness 的释放点，减少 peak slots。
- 优先级：中，高影响，工作量：3-5 天。

PR-7（缓存/守卫）：编译缓存 + shape guard
- 目标：实现编译产物缓存 keyed by 子图签名 + guard（运行时检查形状满足符号约束）。
- 修改位置：
  - [examples/tensor_cpu/runtime.py]（扩展 `JITModule` / `JITEngine`）
  - 新文件 [examples/tensor_cpu/compile_cache.py]
- 示例改动：在 `JITEngine.compile_graph` 前检查缓存；生成 kernel 时同时输出 guard 条件插入到 run 逻辑。
- 优先级：中，工作量：1-2 天。

PR-8（调度）：成本模型 + Lazy Executor（简单版）
- 目标：实现一个轻量成本模型并在捕获阶段决定是否 fusion/compile 或 fall back 到 eager。
- 修改位置：
  - 新文件 [examples/tensor_cpu/scheduler.py]
  - 集成点 [examples/tensor_cpu/capture.py] 或 `nn/jit.py`
- 示例改动：提供 `should_compile(subgraph) -> bool` 基于 op count & estimated FLOPs。
- 优先级：中，工作量：2-4 天。

PR-9（下沉/Codegen）：把融合子图下沉到 `CppCodegen`
- 目标：让 fusion 生成的 FUSED 节点被 `CppCodegen` 正确 emit，为性能提供路径。
- 修改位置：
  - [examples/tensor_cpu/codegen.py]（新增 `_emit_fused_*`）
- 示例改动：实现 `_emit_fused_matmul_bias_relu` 使用现有 `_emit_fused_matmul` 逻辑。
- 优先级：中高，工作量：2-4 天。

PR-10（autotune）：HPC template 参数搜索
- 目标：实现简单 autotuner：对小枚举参数（tile sizes、BM/BN/BK）做短测并缓存最优参数。
- 修改位置：
  - [examples/tensor_cpu/autotune.py]
  - 调用点：`compile_training_step` 或 `CppCodegen` 在 detect matmul size 时触发。
- 优先级：低中，工作量：2-4 天。

PR-12（质量）：profiling、测试、示例、文档
- 目标：添加基准（matrix multiply、transformer block）、单元测试、CI 配置、README demo。
- 修改位置：
  - [examples/benchmarks/], [examples/tests/], [readme.md]
- 优先级：高（必须），工作量：2-5 天。

简历里适合写的项（按影响力优先列出）
- 功能性陈述（必写）：设计并实现“惰性图捕获 + 首次按需 JIT 编译” —— 在 repo 中实现 `LazyTensor.materialize()`、`LazyJITTrainer` 缓存逻辑与编译流水线（链接到 PR-1/2/3）。
- 性能提升（强烈建议量化）：实现 operator fusion + codegen 优化，使关键模型速度提升 X%（说明基准，如 small MLP/MatMul，提供前后吞吐对比和配置），对应 PR-5 + PR-9。
- 内存优化（很好展示）：实现 liveness-driven memory planner，降低 peak 内存 Y%（PR-6）。
- 工程 & 可用性：添加 compile cache + shape guards（PR-7），并写完整的 CI/tests/benchmarks（PR-12）。
- 进阶（加分项）：实现 autotuner 或跨后端 lowering（PR-10/9），或实现图级反向优化与 checkpoint（PR-11）。

简历写法示例（一句话+量化）：
- “设计并实现一个轻量级全局惰性 JIT：实现 LazyTensor → Graph 的物化、图优化（常量折叠/CSE/DCE）、pattern-based fusion 与 C++ codegen，减少模型运行时间达 3×（矩阵/MLP 基准），并将 peak 内存降低 40%。”
（记得用真实数字；如果没有，先用合适的 micro-benchmarks 量化再写入简历）

下一步建议（我可以代劳）
- 我可以先实现 PR-4（常量折叠+CSE+DCE），这是收益与实现难度的最佳平衡点；需要我现在开始按 PR-4 写具体代码补丁并添加测试吗？


[高] 修复 HPC matmul 的动态 shape correctness，并补 use_hpc_template=True 的动态 shape 回归测试。收益：避免 silent wrong answer。难度：中。
[高] 收敛支持面：明确哪些是 stable，哪些是 experimental；先把多输出、control flow、pybind、LayeredCodegen 的 API 声明和实现对齐。收益：减少“说得多、跑得少”。难度：中。
[高] 把仓库整理成“从 repo root 可安装、可测试、可 CI”的形态。收益：协作成本和可信度立刻提升。难度：低。
[中] 让 JITEngine 真正支持选择后端，并决定 v1/v2 的去留。收益：架构收敛。难度：高。
[中] 拆分 codegen.py、runtime_pybind.py、tracing_v2.py。收益：可维护性显著提升。难度：中。
[中] 建正式 benchmark harness，加入 NumPy/OpenBLAS 基线、CPU/编译器元数据、perf counter。收益：性能主张才站得住。难度：中。
[低] 明确 dtype 策略：要么真支持 float64，要么彻底限定 float32。收益：接口稳定。难度：低。
[低] 清理 repo hygiene：__pycache__、占位 URL、缺失 README、嵌套 workflow 路径。收益：专业度提升。难度：低。
首先该做的三件事：1 -> 2 -> 3。


**结论**
现在主路径 `Tensor / TraceContext -> Graph -> optimize_graph -> CppCodegen -> JITEngine/runtime` 已经比之前清晰很多，**足够作为“唯一稳定主线”继续推进**。  
但它还没有到“可以放心往上堆功能”的程度，最大风险不是缺功能，而是你想做的几件大事彼此耦合，如果同时推进，项目会重新变乱。

**先定三个铁原则**
1. **只有一条 stable path**。experimental 只能“逐项晋升”，不能整包合并。
2. **只有一套核心 IR**。不能继续长期并存 `Graph` 和另一套 `GraphNode/ControlFlowGraph`。
3. **先建立验证闭环，再做性能和 runtime 下沉**。否则优化越多，越难证明没坏。

**推荐执行顺序**
1. **阶段 0：冻结当前 stable path**
目标：把现有主路径正式定型，停止横向发散。  
交付物：
- `docs/architecture.md` 里明确 stable / experimental 边界
- stable API 清单
- “不支持什么”清单：多输出、control flow、pybind runtime 主路径等
- 所有新功能默认进 feature flag 或 `experimental/`

2. **阶段 1：先做工程底座**
这是你后面所有工作的地基，必须先做。
交付物：
- 根目录 `pyproject.toml`
- 可安装：`pip install -e .`
- 可测试：`pytest` 从仓库根直接跑
- 可 CI：lint + unit + integration + benchmark smoke
- benchmark harness：固定 CPU/线程数/编译器信息/结果输出 JSON
- 回归基线：正确性测试 + 性能基线 + ABI 边界测试  
这一步不性感，但收益最大。

3. **阶段 2：先拆 `CppCodegen`，不要急着接 control flow**
这是当前最值得做的架构改造。
把现在的 `backend/codegen.py` 拆成：
- `shape_solver`
- `memory_planner`
- `op_lowering`
- `cpp_emitter`
- `kernel_metadata`  
目标不是换功能，而是让“同样的主路径”先变得可维护。  
只有这一步做完，后面 layered IR、cache tiling、对齐策略才有自然落点。

4. **阶段 3：统一 IR，逐步吸收 experimental**
顺序建议：
- 先吸收 `codegen_v2` 的“分层 lowering 思路”
- 再处理 `symbolic`
- 最后才做 `control_flow`  
重点不是搬文件，而是**把 experimental 的想法折叠进同一套 IR**。  
我的建议是：
- 保留一个 `Graph`
- 为节点增加 region/subgraph 概念
- `if/while/for` 作为同一 IR 上的高阶 op
- 不再保留独立的 `GraphNode/ControlFlowGraph` 世界  
换句话说：**吸收语义，不保留平行体系**。

5. **阶段 4：建立性能闭环，再做真正优化**
这一步要先有方法，再有实现。
先加：
- correctness guard
- perf benchmark
- profiler 数据采集
- 配置开关  
然后再逐项做：
- 线程策略
- 对齐分配
- cache-aware tile 参数
- SIMD / OpenMP 策略  
建议接口层次：
- `ThreadPolicy`
- `AlignmentPolicy`
- `TilePolicy`
- `KernelConfig`  
不要把这些继续塞回 `CppCodegen` 一个类里。

6. **阶段 5：runtime 下沉到 C++**
这件事应该在“IR 和 codegen 边界稳定之后”做，不要提前。
目标结构：
- Python：前端、trace、pass、编译调度
- C++：runtime、workspace、allocator、kernel loader、线程执行、ABI 校验
- Python 只保留 thin binding  
建议先做：
- 稳定 `TensorDesc` / kernel ABI
- C++ runtime library
- pybind11 或 nanobind thin wrapper
- 保留 ctypes 作为 fallback 一段时间  
不要一上来就全替换。

**不建议的做法**
- 同时推进 `control_flow + runtime 下沉 + HPC 优化 + layered IR`
- 把 experimental 文件直接挪进 stable
- 在没有 benchmark/guard 的情况下改 tile、线程、cache 策略
- 继续让 `backend/codegen.py` 承担 shape、memory、lowering、emitter、tuning 五种职责

**你现在最该先做的 3 件事**
1. **做阶段 1：根级安装、测试、CI、benchmark 基线**
2. **做阶段 2：拆 `CppCodegen`**
3. **做阶段 3 的第一步：把 `codegen_v2` 的分层思想并入 stable backend，不引入 control flow**

**建议的里程碑**
1. `M1` 工程底座完成：可安装、可测试、可 CI、可跑 benchmark
2. `M2` stable backend 拆层完成：主路径功能不变但结构清晰
3. `M3` layered backend 接管 stable codegen
4. `M4` 性能策略框架上线：线程/对齐/tile/config 可配置
5. `M5` C++ runtime 替换 ctypes 主路径
6. `M6` control flow 进入 unified IR，并先支持最小子集




**1. 第一优先级：吸收 layered/lowering 的思想，不先吸收 control flow**
如果 experimental 里有更分层的 lowering / IR 设计，这个最值得优先吸收。  
原因很简单：  
你现在 stable backend 已经拆成了：
- shape solver
- memory planner
- op lowering
- cpp emitter

这正好是承接更结构化 lowering 的好时机。  
所以最合适的方向不是先碰控制流，而是先把 experimental 里“更好的后端抽象”并入 stable backend。

一句话说：  
**先吸收 backend 架构思想，再吸收语言特性。**

**2. 第二优先级：吸收 symbolic / shape abstraction**
这个也比较适合，因为它和现有 stable path 已经有接壤点。  
现在你的 runtime、shape solver、benchmark、HPC exact shape guard 都已经逼着你去更认真地处理 shape 语义了。  
所以如果 experimental 里有更成熟的 symbolic 设计，可以逐步吸收进：
- shape inference
- output_sym_shape
- workspace sizing
- dynamic shape reuse policy

但要注意：  
**吸收的是 shape model，不是把整个 symbolic 实验模块整包并回来。**

**3. 第三优先级：最后才考虑 control flow**
control flow 是最容易把主路线重新搞乱的。  
因为它不是加几个 op 那么简单，它会直接冲击：

- IR 结构
- tracing 语义
- pass 语义
- codegen 语义
- backward graph 构造
- runtime 执行模型

尤其你之前已经意识到一个大问题：  
`Graph` 和 `GraphNode/ControlFlowGraph` 不能长期并存。  
所以如果以后做 control flow，应该是：

- 基于同一套 `Graph IR` 扩展 region/subgraph 语义
- 而不是把 experimental/control_flow 那套直接并回来

